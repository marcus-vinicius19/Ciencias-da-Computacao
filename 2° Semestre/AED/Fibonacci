#include <stdio.h>

int somatoria(int n)
{
    int soma = 0, i;
    for(i = 1 ; i <= n ; i++ )
    {
        soma += 5 * (i*i) + 2 * i + 8;
    }
    return soma;
}

int main(void) 
{
    int n;   
    scanf("%d", &n);
    printf("Somatoria = %d", somatoria(n));
    return 0;
}
